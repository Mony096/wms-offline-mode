import 'package:flutter/material.dart';

// color
const PRIMARY_COLOR = Color.fromARGB(255, 10, 13, 85);
const PRIMARY_BG_COLOR = Color.fromARGB(255, 241, 241, 241);

// Constant
double spaceY(BuildContext context) => MediaQuery.of(context).size.width * 0.09;

// Size
Size size(BuildContext context) => MediaQuery.of(context).size;
